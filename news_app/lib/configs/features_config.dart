/* App Specific Features
  These are now fetched from backend via API
  These constants are used as fallback when API is unavailable
  To enable/disable features, go to admin panel:
  http://localhost/admin/advanced-settings.php
*/

// Multi-language
const bool isMultilanguageEnbled = true;

// Facebook Login - Controlled by backend
const bool isFacebookLoginEnabled = false;

// Google Login - Controlled by backend
const bool isGoogleLoginEnabled = false;

// Phone Number Login
const bool isPhoneNumberLoginEnabled = false;

// Apple Login
const bool isAppleLoginEnabled = true;

// Splash Icon Animations
const bool splashIconAnimationEnabled = false;



